spring:
  application:
    name: publication
  data:
    mongodb:
      host: localhost
      port: 27017
      authentication-database: admin
      username: root
      password: example
      database: circuit-breaker

management:
  endpoints:
    web:
      exposure:
        include: "*"

client:
  comments:
    url: http://localhost:8082